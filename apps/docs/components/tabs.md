---
pageClass: component-docs-page
---

<!-- This file is generated by scripts/commands/generate-docs.ts. Do not edit manually. -->

# Tabs

Styled tabs component family built on zw-tabs primitives.

<div class="zw-badge-row">
  <span class="zw-badge">@zeus-web/tabs</span>
  <span class="zw-badge">Navigation</span>
  <span class="zw-badge">primitive-only</span>
  <span class="zw-badge">Generated</span>
</div>

## Playground

<ClientOnly>
<ComponentPlayground name="tabs" />
</ClientOnly>

### Source

Choose the framework entry that matches your application.

::: code-group

```html [Web Component]
<script type="module">
  import '@zeus-web/tabs/wc/auto'
</script>

<zw-tabs default-value="overview">
  <zw-tabs-list>
    <zw-tabs-trigger value="overview">Overview</zw-tabs-trigger>
    <zw-tabs-trigger value="activity">Activity</zw-tabs-trigger>
  </zw-tabs-list>
  <zw-tabs-content value="overview">Project overview</zw-tabs-content>
  <zw-tabs-content value="activity">Recent activity</zw-tabs-content>
</zw-tabs>
```

```tsx [React]
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@zeus-web/tabs/react'

export function TabsExample() {
  return (
    <Tabs defaultValue="overview">
      <TabsList>
        <TabsTrigger value="overview">Overview</TabsTrigger>
        <TabsTrigger value="activity">Activity</TabsTrigger>
      </TabsList>
      <TabsContent value="overview">Project overview</TabsContent>
      <TabsContent value="activity">Recent activity</TabsContent>
    </Tabs>
  )
}
```

```vue [Vue]
<script setup lang="ts">
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@zeus-web/tabs/vue'
</script>

<template>
  <Tabs default-value="overview">
    <TabsList>
      <TabsTrigger value="overview">Overview</TabsTrigger>
      <TabsTrigger value="activity">Activity</TabsTrigger>
    </TabsList>
    <TabsContent value="overview">Project overview</TabsContent>
    <TabsContent value="activity">Recent activity</TabsContent>
  </Tabs>
</template>
```

:::

## Install

This component does not have a registry template yet.

Install the primitive package instead:

```bash
pnpm add @zeus-web/tabs
```

## Imports

Styled registry component:

Not available yet.

Primitive React wrapper:

```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@zeus-web/tabs/react'
```

Web Component entry:

```ts
import '@zeus-web/tabs/wc/auto'
```

## Props

| Prop           | Type                                                    | Default      | Description                  |
| -------------- | ------------------------------------------------------- | ------------ | ---------------------------- |
| `value`        | `string`                                                | -            | Controlled active tab value. |
| `defaultValue` | `string`                                                | -            | Initial active tab value.    |
| `orientation`  | `TabsOrientation`<br />Values: `horizontal`, `vertical` | `horizontal` | Tabs orientation.            |
| `disabled`     | `boolean`                                               | -            | Disables all triggers.       |

## Events

| Event          | React prop      | Detail                              | Description                      |
| -------------- | --------------- | ----------------------------------- | -------------------------------- |
| `value-change` | `onValueChange` | `value: string, nativeEvent: Event` | Emitted when active tab changes. |

## Slots

| Slot      | Description            |
| --------- | ---------------------- |
| `default` | Tabs child components. |

## Styling

Uses Tailwind: yes

Theme tokens:

- `bg-muted`
- `text-muted-foreground`
- `ring-ring`

Internal selectors:

- `[data-slot=tabs-list]`
- `[data-slot=tabs-trigger]`
- `[data-slot=tabs-content]`

## Registry

Registry source: not available yet.

The `tabs` primitive is available through `@zeus-web/tabs`, but it is not currently installable through the CLI registry.

Use the primitive package directly until a registry template is added.

## AI usage rules

Do:

- Use Tabs, TabsList, TabsTrigger and TabsContent together.
- Keep trigger value and content value aligned.

Do not:

- Do not use TabsContent without matching TabsTrigger value.

## Examples

### React styled usage

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@zeus-web/tabs/react'

export function Example() {
  return (
    <Tabs defaultValue="account">
      <TabsList>
        <TabsTrigger value="account">Account</TabsTrigger>
        <TabsTrigger value="password">Password</TabsTrigger>
      </TabsList>
      <TabsContent value="account">Account panel</TabsContent>
      <TabsContent value="password">Password panel</TabsContent>
    </Tabs>
  )
}
```
