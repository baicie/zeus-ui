---
pageClass: component-docs-page
---

<!-- This file is generated by scripts/commands/generate-docs.ts. Do not edit manually. -->

# 单选框组

基于 zw-radio-group 基础组件构建的样式化单选组。

<div class="zw-badge-row">
  <span class="zw-badge">@zeus-web/radio-group</span>
  <span class="zw-badge">数据录入</span>
  <span class="zw-badge">仅基础组件</span>
  <span class="zw-badge">自动生成</span>
</div>

## 交互演示

<ClientOnly>
<ComponentPlayground name="radio-group" />
</ClientOnly>

### 源码

请选择与你的应用框架匹配的入口。

::: code-group

```html [Web Component]
<script type="module">
  import '@zeus-web/radio-group/wc/auto'
</script>

<zw-radio-group default-value="team">
  <zw-radio-group-item value="personal">Personal</zw-radio-group-item>
  <zw-radio-group-item value="team">Team</zw-radio-group-item>
</zw-radio-group>
```

```tsx [React]
import { RadioGroup, RadioGroupItem } from '@zeus-web/radio-group/react'

export function RadioGroupExample() {
  return (
    <RadioGroup defaultValue="team">
      <RadioGroupItem value="personal">Personal</RadioGroupItem>
      <RadioGroupItem value="team">Team</RadioGroupItem>
    </RadioGroup>
  )
}
```

```vue [Vue]
<script setup lang="ts">
import { RadioGroup, RadioGroupItem } from '@zeus-web/radio-group/vue'
</script>

<template>
  <RadioGroup default-value="team">
    <RadioGroupItem value="personal">Personal</RadioGroupItem>
    <RadioGroupItem value="team">Team</RadioGroupItem>
  </RadioGroup>
</template>
```

:::

## 安装

此组件暂时没有 Registry 模板。

请改为安装基础组件包：

```bash
pnpm add @zeus-web/radio-group
```

## 导入

样式化 Registry 组件：

暂不可用。

基础组件 React 包装器：

```tsx
import { RadioGroup, RadioGroupItem } from '@zeus-web/radio-group/react'
```

Web Component 入口：

```ts
import '@zeus-web/radio-group/wc/auto'
```

## 属性

| 属性           | 类型                                                          | 默认值     | 说明                 |
| -------------- | ------------------------------------------------------------- | ---------- | -------------------- |
| `value`        | `string`                                                      | -          | 受控选中值。         |
| `defaultValue` | `string`                                                      | -          | 初始选中值。         |
| `name`         | `string`                                                      | -          | 原生单选组名称。     |
| `orientation`  | `RadioGroupOrientation`<br />可选值: `horizontal`, `vertical` | `vertical` | 布局方向。           |
| `required`     | `boolean`                                                     | -          | 将单选组标记为必填。 |

## 事件

| 事件           | React 属性      | 详情                                | 说明               |
| -------------- | --------------- | ----------------------------------- | ------------------ |
| `value-change` | `onValueChange` | `value: string, nativeEvent: Event` | 选中值变化时触发。 |

## 插槽

| 插槽      | 说明                    |
| --------- | ----------------------- |
| `default` | RadioGroupItem 子组件。 |

## 样式

使用 Tailwind: 是

主题变量：

- `text-primary`
- `ring-ring`

内部选择器：

- `[data-slot=radio-group-control]`
- `[data-slot=radio-group-label]`

## Registry

Registry 源码：暂不可用。

`radio-group` 基础组件可通过 `@zeus-web/radio-group` 使用，但目前还不能通过 CLI Registry 安装。

在 Registry 模板加入之前，请直接使用基础组件包。

## AI 使用规则

建议：

- 多选一场景使用 RadioGroup。
- 保持 RadioGroupItem 的值唯一。

避免：

- 只能选择一个选项时不要使用 Checkbox。

## 示例

### React 样式化用法

```tsx
import { RadioGroup, RadioGroupItem } from '@zeus-web/radio-group/react'

export function Example() {
  return (
    <RadioGroup defaultValue="email">
      <RadioGroupItem value="email">Email</RadioGroupItem>
      <RadioGroupItem value="sms">SMS</RadioGroupItem>
    </RadioGroup>
  )
}
```
