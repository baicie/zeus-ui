<!-- This file is generated by scripts/commands/generate-docs.ts. Do not edit manually. -->

# Badge

Styled badge component built on the zw-badge primitive.

<div class="zw-badge-row">
  <span class="zw-badge">@zeus-web/badge</span>
  <span class="zw-badge">primitive-only</span>
  <span class="zw-badge">Generated</span>
</div>

## Install

This component does not have a registry template yet.

Install the primitive package instead:

```bash
pnpm add @zeus-web/badge
```

## Imports

Styled registry component:

Not available yet.

Primitive React wrapper:

```tsx
import { Badge } from '@zeus-web/badge/react'
```

Web Component entry:

```ts
import '@zeus-web/badge/wc'
```

## Props

| Prop      | Type                                                                                          | Default   | Description           |
| --------- | --------------------------------------------------------------------------------------------- | --------- | --------------------- |
| `variant` | `BadgeVariant`<br />Values: `default`, `secondary`, `outline`, `danger`, `success`, `warning` | `default` | Visual style variant. |
| `size`    | `BadgeSize`<br />Values: `sm`, `md`, `lg`                                                     | `md`      | Badge size.           |

## Events

No public events documented.

## Slots

| Slot      | Description    |
| --------- | -------------- |
| `default` | Badge content. |

## Styling

Uses Tailwind: yes

Theme tokens:

- `bg-primary`
- `text-primary-foreground`
- `bg-secondary`

Internal selectors:

- `[data-slot=badge-root]`

## Registry

Registry source: not available yet.

The `badge` primitive is available through `@zeus-web/badge`, but it is not currently installable through `zweb add badge`.

Use the primitive package directly until a registry template is added.

## AI usage rules

Do:

- Use Badge for status labels and counts.

Do not:

- Do not use Badge as a clickable button.

## Examples

### React styled usage

```tsx
import { Badge } from '@/components/ui/badge'

export function Example() {
  return <Badge>New</Badge>
}
```
