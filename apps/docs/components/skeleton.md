<!-- This file is generated by scripts/commands/generate-docs.ts. Do not edit manually. -->

# Skeleton

Styled skeleton component built on the zw-skeleton primitive.

<div class="zw-badge-row">
  <span class="zw-badge">@zeus-web/skeleton</span>
  <span class="zw-badge">zweb add skeleton</span>
  <span class="zw-badge">Generated</span>
</div>

## Add

```bash
zweb add skeleton
```

## Imports

Styled component:

```tsx
import { Skeleton } from '@/components/ui/skeleton'
```

Primitive React wrapper:

```tsx
import { Skeleton } from '@zeus-web/skeleton/react'
```

Web Component entry:

```ts
import '@zeus-web/skeleton/wc'
```

## Props

| Prop       | Type                                                    | Default | Description                |
| ---------- | ------------------------------------------------------- | ------- | -------------------------- |
| `variant`  | `SkeletonVariant`<br />Values: `text`, `rect`, `circle` | `rect`  | Shape variant.             |
| `animated` | `boolean`                                               | `true`  | Enables loading animation. |

## Events

No public events documented.

## Slots

No slots documented.

## Styling

Uses Tailwind: yes

Theme tokens:

- `bg-muted`

Internal selectors:

- `[data-slot=skeleton-root]`

## Registry

Type: `registry:ui`

Dependencies:

- `@zeus-web/skeleton`
- `class-variance-authority`
- `clsx`
- `tailwind-merge`

Files:

- `default/lib/utils.ts` → `lib/utils.ts` (`registry:lib`)
- `default/skeleton.tsx` → `components/ui/skeleton.tsx` (`registry:ui`)

## AI usage rules

Do:

- Use Skeleton for loading placeholders.

Do not:

- Do not show Skeleton for content that loads instantly.

## Examples

### React styled usage

```tsx
import { Skeleton } from '@/components/ui/skeleton'

export function Example() {
  return <Skeleton className="h-4 w-12" />
}
```
