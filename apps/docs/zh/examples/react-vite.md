# React Vite 示例

React Vite 示例用于验证 Registry 风格的 React 使用路径。

运行：

```bash
pnpm --filter @zeus-web/example-react-vite dev
```

构建：

```bash
pnpm --filter @zeus-web/example-react-vite build
```

该示例使用本地 `src/components/ui/*` wrappers，并从 `@zeus-web/<component>/react` 导入生成的 primitive React 入口。
